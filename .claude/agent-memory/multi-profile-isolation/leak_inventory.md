---
name: leak-inventory
description: Ranked cross-profile state-bleed inventory for claudeline-core (statusline + usage hooks)
metadata:
  type: project
---

**SUPERSEDED IN PART as of v0.7.0 (2c32e0b, 2026-07-10, branch cf/v0.7.0-stdin-rate-limits) — see [[v070_stdin_rate_limits_review]] for the full re-verification.** Items 1 and 3 below describe deleted code (`hooks/show-usage-limits.sh` and its `USAGE_REFRESH_HOOK` resolver no longer exist — CC ≥2.1.80 now hands each session its own usage on stdin, so there's nothing left to fetch or refresh). Kept here as history, not as live findings. Original ranking below preserved verbatim; v0.7.0 status noted inline.

Ranked, worst first, verified against code at /Users/chadfurman/code/claudeline on 2026-07-06:

1. **CRITICAL — keychain single-slot (macOS only).** `hooks/show-usage-limits.sh` line 41 reads `security find-generic-password -s "Claude Code-credentials"` — one global entry, not per `CLAUDE_CONFIG_DIR`. Last `/login` wins across profiles. Upstream: anthropics/claude-code#20553 (re-verify still open before trusting this framing — do not assume from memory). Code has an inline comment (lines 36-39) documenting this constraint explicitly rather than working around it. Linux is NOT affected: line 47 reads `${_CREDS_DIR}/.credentials.json` (a file under `$CLAUDE_CONFIG_DIR`), properly per-profile — the clean reference model.
   **v0.7.0: CLOSED BY CONSTRUCTION.** `hooks/show-usage-limits.sh` is deleted (confirmed: `hooks/` directory no longer exists in the tree). No `security find-generic-password` call remains anywhere in the live codebase (verified by grep across `*.sh`/`*.md`, excluding CHANGELOG.md and agent-memory history files). There is no fetch step left to leak from — usage is stdin-only.
2. **HIGH — PR cache not account-keyed.** `statusline-command.sh` line 249: `local cache="/tmp/.claude_pr_cache_${repo_name}"` — keyed by repo name only, no account/`ACCOUNT_ID` component. Same repo open under both profiles shares one cache entry. Confirmed still true in this repo's `get_pr_number()` as of 2026-07-06. Note: per domain-bootstrap researcher notes, the user's *deployed* dotfiles copy is reportedly ahead of this repo and already keys this by `_${ACCOUNT_ID}` — if asked to fix this, check the deployed copy for the already-written fix before writing a new one.
   **v0.7.0 fix-pass (a9ab2d0): PARTIALLY REOPENED.** Keying itself confirmed correct (`_${ACCOUNT_ID}` suffix) but the write was unguarded against `ACCOUNT_ASSUMED` — see [[v070_fixpass_regate]] for that repro.
   **v0.7.0 (f6b971e, re-gated 2026-07-10): CLOSED.** `get_pr_number()` now early-returns under `((ACCOUNT_ASSUMED))` *before* the cache/branch_cache/lock variables are even declared — live-reproduced writing ZERO files under a guessed identity (not just the cache, the branch-cache and lock too). Now covered by `scripts/test.sh` (`test_assumed_identity_pr_cache_never_written`, `test_explicit_identity_pr_cache_still_written`, `test_assumed_identity_stdin_pr_no_cache_no_gh`). See [[v070_regate2]] Verdict 2 for the repro.
3. **MEDIUM — hardcoded hook path.** `statusline-command.sh` line 76: `readonly USAGE_REFRESH_HOOK="$HOME/.claude/hooks/show-usage-limits.sh"` — always resolves to the work-profile hook path, never `$CLAUDE_CONFIG_DIR`-relative. Compensated: that hook re-derives its own `ACCOUNT_ID`/`_CREDS_DIR` internally from `CLAUDE_CONFIG_DIR` (lines 6-12), so despite running "the work copy of the script," it still writes to the correct per-account cache file (`/tmp/.claude_usage_limits_${ACCOUNT_ID}.json`). Verified this compensation holds in current code — the hook does not trust its own invocation path for identity, only the env var.
   **v0.7.0: CLOSED BY CONSTRUCTION.** `USAGE_REFRESH_HOOK`/`resolve_usage_refresh_hook` are deleted along with the hook itself — nothing left to hardcode a path to.
4. **Env-detection defaults to "work."** Both scripts: `statusline-command.sh` lines ~51-55 and `hooks/show-usage-limits.sh` lines ~6-12 test `CLAUDE_CONFIG_DIR == *"claude-personal"*`, else default to `"work"`/`$HOME/.claude`. Any launch without `CLAUDE_CONFIG_DIR` set (desktop app launches, background jobs, resumed sessions missing the shell alias) silently renders as work profile.
   **v0.7.0: STILL LIVE — reclassified MEDIUM-HIGH, now the top remaining item.** `statusline-command.sh:50-59`, unchanged detection logic (sole copy now, no hook to keep in sync). Pre-v0.7.0 this mislabeling mattered for keychain-fetch attribution, but was largely masked by the bigger CRITICAL bug (numbers were already wrong for a bigger reason). Post-v0.7.0, with the CRITICAL bug closed, this is the ONLY remaining conduit for state-bleed: `write_usage_cache()` at `statusline-command.sh:117-129` is called unconditionally from `get_usage_limits()` (line 175) with NO `ACCOUNT_ASSUMED` guard — an assumed-identity session (real backend identity ambiguous to this script, but its own `rate_limits` stdin payload is session-authoritative for whatever CC actually authenticated it as) writes straight into whichever account's cache file `detect_account()` guesses, silently overwriting real data if the guess is wrong. See [[v070_stdin_rate_limits_review]] for the full reachability/fix writeup — this is now the load-bearing finding of the whole domain.
5. **install.sh / settings-example.json are single-profile by construction.** `install.sh` line 8 hardcodes `CLAUDE_DIR="$HOME/.claude"`; `settings-example.json` hardcodes `~/.claude/...` command paths (lines 5, 11, 15). No path in either file ever targets `~/.claude-personal`. A second profile is entirely manual user setup — install.sh has zero multi-profile awareness and no upgrade/reconciliation story.
   **v0.7.0: STILL LIVE, unchanged.** `install.sh:8` still `CLAUDE_DIR="${CLAUDE_CONFIG_DIR:-$HOME/.claude}"`. v0.7.0 removed the hook/capture-script install lines and the `hooks.SessionStart` wiring (nothing to install anymore), but didn't touch the single-`$HOME/.claude`-default framing. `settings-example.json` now only wires `statusLine.command` — no `hooks` key at all.

See [[abandoned-keychain-attempt]] for the history of item 1, [[diff-test-recipe]] for how to verify any of these live (recipe itself is now STALE post-v0.7.0 — see [[v070_stdin_rate_limits_review]] for the replacement), [[2026-07-06-incident]] for the live incident that seeded this memory, and [[v070_stdin_rate_limits_review]] for the current-state re-verification and the one open fix.
