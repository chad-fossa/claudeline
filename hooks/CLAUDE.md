# hooks

SessionStart/background hook that fetches and caches usage limits (`show-usage-limits.sh`) — half of the statusline render+refresh cycle with `../statusline-command.sh` and `../skills/usage/`.

consult-expert: claudeline-core
